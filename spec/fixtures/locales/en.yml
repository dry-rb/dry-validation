en:
  errors:
    filled?: "%{name} can't be blank"
    size?:
      default: "size must be %{num}"
      range: "size must be between %{left} and %{right}"
    attributes:
      email:
        filled?: "Please provide your email"
