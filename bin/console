#!/usr/bin/env ruby
# frozen_string_literal: true

require 'optparse'
require 'bundler/setup'
require 'dry-validation'

if ARGV.include?('--pry')
  require 'pry'
  Pry.start
else
  require 'irb'
  IRB.start
end
